# 第6章　依存を定義する

## 1. 依存グラフはModule間の直接利用として表す

Pythonにおける依存グラフは、基本的に「どのModuleが、どのModuleの公開要素をimportし、利用しているか」という関係である。

具象設計論では、この直接利用の関係を依存の単位にする。アプリケーション全体の依存を、別の階層的なDependency Objectへ写し替えることを前提にしない。

Pythonであれば、基本的には**一つの`.py`ファイル、すなわち一つのモジュール**である。

この考え方はUseCaseだけに適用する規則ではない。Feature、Data Component、Entry、外部サービスなど、すべてのModuleに同じように適用する。

例えば、

```text
feature/
    complete_job/
        usecase.py
```

の`usecase.py`が、

- Connection
- Job Data Componentの公開関数
- 現在時刻
- 一つの設定値

を直接利用するなら、そのモジュールに必要な依存だけをそのモジュールで定義する。

一方、Job Data Component内部の別モジュールがRepositoryを利用するなら、そのRepository依存はJob側のモジュールで別に定義する。

依存は上位から下位へ階層的に受け渡すものではない。下位Moduleの成立条件は下位Module自身が扱い、上位Moduleは自分が直接利用する公開面だけを見る。

**依存を直接利用する場所で、その依存だけを明示する。**

---

## 2. 関数引数もDependency Injectionである

Dependency Injectionという言葉から、DI ContainerやInterfaceの登録機構を想像する必要はない。

例えば、

```python
def calculate_deadline(
    start,
    holidays,
):
    ...
```

という関数がある。

`holidays`が外部から与えられ、テスト時に別の値へ差し替えられるなら、すでに依存は明示されている。

```python
calculate_deadline(
    start,
    fake_holidays,
)
```

とテストできる。

依存性注入の本質は、

> 必要なものを外部から受け取れること

である。

具象設計論では、関数引数だけで十分なら、それ以上の仕組みを追加しない。

差し替える必要のない依存は、通常のimportとして明示されていてもよい。直接依存であることと、すべてをDependency型へ包むことは同じではない。

---

## 3. 複数の依存を一つの型で表現する

一つのモジュールが複数の依存を利用するようになると、個別の引数として並べ続けることが読みづらくなる場合がある。

例えば、

```python
def execute(
    require_arg,
    param1,
    conn,
    data,
):
    ...
```

とすることもできる。

しかし、この三つが、

> このModuleが標準的に利用する外部要素

としてまとまっているなら、型として表現できる。

Pythonでは`dataclass`を使える。

```python
from __future__ import annotations

from dataclasses import dataclass
from typing import Self


@dataclass(frozen=True, slots=True)
class Dependencies:
    param1: int
    conn: Connection
    data: Data
```

この型は、アプリケーション全体のContainerではない。

**このモジュールが利用側として期待しているものの形を、そのまま定義したもの**である。

---


## 4. 関数をDependencyとして受け取るときの型

Pythonでは、関数そのものをDependencyとして受け取る場合、その引数名や意味までを通常のクラス型のようにそのまま型注釈へ持ち込むことはできない。

例えば、利用側が期待しているのが、

```python
def complete_job(
    job_id: int,
    connection: Connection,
) -> None:
    ...
```

という関数だとする。

Dependency側では、

```python
from collections.abc import Callable


CompleteJob = Callable[
    [int, Connection],
    None,
]
```

のように、呼び出し可能な「口」の形へ合わせて型を定義できる。

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    complete_job: CompleteJob
```

ただし`Callable`は、引数名やより複雑な呼び出し規約を表現する能力が限定的である。

その制約が問題になるなら、`Protocol`で呼び出し可能オブジェクトのInterfaceを定義する方法もある。

```python
from typing import Protocol


class CompleteJob(Protocol):
    def __call__(
        self,
        job_id: int,
        connection: Connection,
    ) -> None:
        ...
```

ここでInterfaceを導入する理由は、「依存だからInterfaceにする」ためではない。

**Pythonの型システム上、利用側が期待する呼び出し口をより正確に表現したいから**である。

単純な関数なら`Callable`で十分なことが多い。引数名、Keyword-only、Overloadなどまで契約として表したいなら`Protocol`を使う。


## 5. `dataclass`は利用側の想定を型として表す

Dependencyの型定義では、外部の実装がどのようなクラス階層を持っているかよりも、利用するモジュールが何を必要としているかを重視する。

例えば、

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    data: Data
```

と書くなら、このUseCaseが必要としているのは`Data`である。

その`Data`が、

- Databaseから取得された
- APIから取得された
- Cacheから取得された
- 複数の形式を変換して作られた

といった事情をUseCaseが知る必要はない。

利用側で必要な結果の形を、そのままDependencyの型として表現する。

これは、依存の存在を抽象化して隠すためではない。

**利用側が何を期待しているかを具体化するため**である。

---

## 6. `default()`でそのモジュールの標準構成を示す

多くの業務システムでは、Productionで使う依存は頻繁には変わらない。

あるUseCaseでは、通常このConnection Factoryを使う。このData Providerを使う。この設定値を使う。

毎回外部からそれらを組み立てる必要がないなら、モジュール自身が標準構成を持ってよい。

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    param1: int
    conn: Connection
    data: Data

    @classmethod
    def default(cls) -> Self:
        return cls(
            param1=10,
            conn=connection_factory(),
            data=data_provider(),
        )
```

`default()`は、このモジュールの通常構成を表す。

これは単なる便利関数ではない。

> 通常このモジュールは何を使って動くのか

をコード上で明示する。

---

## 7. Pythonでは`Dependencies.default()`をデフォルト引数へ直接置かない

Pythonでは、関数のデフォルト引数は関数呼び出し時ではなく、関数定義時に評価される。

したがって、次の形は基本形にしない。

```python
def execute(
    require_arg: int,
    *,
    dependencies: Dependencies = Dependencies.default(),
):
    ...
```

この場合、`Dependencies.default()`はモジュール読み込み時に一度評価され、その結果が以後の呼び出しで共有される。

Connectionのような実行単位ごとに生成したいものがあれば、この評価時点は明らかに適さない。

そこで、

```python
def execute(
    require_arg: int,
    *,
    dependencies: Dependencies | None = None,
):
    if dependencies is None:
        dependencies = Dependencies.default()

    ...
```

とする。

これなら標準構成はUseCaseを呼び出した時点で生成される。

この`None`を用いた書き方は具象設計論そのものの原則ではない。

**Pythonのデフォルト引数評価規則に合わせて、標準構成を呼び出し時に生成するための実装方法**である。

---

## 8. DependencyはModuleのトップレベルに置く

依存定義は、関数の深い内部に隠さない。

例えば、

```python
# usecase.py

@dataclass(frozen=True, slots=True)
class Dependencies:
    param1: int
    conn: Connection
    data: Data

    @classmethod
    def default(cls) -> Self:
        ...


def execute(
    require_arg: int,
    *,
    dependencies: Dependencies | None = None,
):
    ...
```

とする。

ファイルを開いたときに、そのモジュールが何に依存しているかを上部で確認できる。

具象設計論では、一つのモジュールをできるだけ単体で読めることを重視する。

依存も、そのモジュールを理解するための情報の一部である。

---

## 9. Dependencyの階層構造を作らない

Complete Job FeatureがJob Data Componentを利用し、Job Data ComponentがRepositoryを利用するとする。

ここで、

```python
@dataclass(frozen=True, slots=True)
class CompleteJobDependencies:
    job: JobDependencies
```

のように依存を階層化しない。

さらに、

```text
Feature Dependencies
    ↓
Data Component Dependencies
        ↓
Repository Dependencies
            ↓
Database Dependencies
```

という構造を作らない。

Featureが直接利用するのがJob Data Componentの公開関数なら、Featureではその公開関数だけをDependencyとして持つ。

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    complete_job: CompleteJob
    conn: Connection
```

Job側のモジュールがRepositoryを直接利用するなら、Job側でRepositoryだけを定義する。

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    repository: JobRepository
```

両者は独立したDependencyである。

多くの場合、直接依存の大半は配下概念の公開関数や標準機能である。特別な抽象を増やすのではなく、通常のimport関係と、必要な箇所だけに置いた`Dependencies`から依存グラフを読める状態にする。

---

## 10. 配下のモジュールが使う依存を上位へ運ばない

例えば、

```text
feature/complete_job/usecase.py
    ↓
data_component/job/service.py
    ↓
data_component/job/repository.py
```

という呼び出し関係がある。

UseCaseが利用するのはJobの公開関数である。

Repositoryを直接利用していないなら、FeatureのDependenciesへRepositoryを追加しない。

望ましくない例は、

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    complete_job: CompleteJob
    repository: JobRepository
    conn: Connection
```

である。

`repository`をFeature自身が使わないなら、Featureから見た依存ではない。

Job ServiceがRepositoryを必要とするなら、Job Service自身のDependencyとして定義する。

Clock、UUID生成、乱数、Data Providerなどでも考え方は同じである。

**必要な依存は、その依存を直接使う最小のモジュールに置く。**

---

## 11. `default()`の内部でFactoryやProviderを使ってよい

標準構成を作るには、値の生成処理が必要になることがある。

例えば、

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    conn: Connection
    data: Data

    @classmethod
    def default(cls) -> Self:
        return cls(
            conn=connection_factory(),
            data=data_provider(),
        )
```

とする。

UseCaseが必要なのは`Connection`と`Data`である。

`connection_factory`や`data_provider`をUseCaseの依存にする必要はない。

標準構成を作るための生成処理でしかないなら、`default()`の内部で使えばよい。

これによってUseCase内部に、

```python
provider = ...
raw = provider.load(...)
data = convert(raw)
```

のような、依存を成立させるためだけの処理を持ち込まずに済む。

---

## 12. 結果を渡すかFactoryを渡すか

すべてのFactoryを`default()`で実行すればよいわけではない。

判断基準は、

> そのモジュール自身が生成タイミングを決める必要があるか

である。

一つのUseCase実行で一つのConnectionを使うなら、

```python
conn=connection_factory()
```

として結果をDependencyへ入れられる。

一方、UseCaseの途中で複数のConnectionを作ること自体に意味があるなら、

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    connection_factory: ConnectionFactory
```

とFactoryそのものを渡す。

利用側が必要としているのが結果なのか、生成能力なのかを区別する。

---

## 13. 条件分岐は条件分岐として扱う

依存の選択が条件によって変わることもある。

例えば、

```python
if command.preview:
    sender = preview_sender
else:
    sender = normal_sender
```

とする。

この時点で、特別な設計用語やクラスを作る必要はない。

単なる条件分岐で説明できるなら、条件分岐として書く。

同じ選択規則が複数箇所へ現れ、独立した意味を持つことが分かってから、新しい関数やモジュールとして切り出せばよい。

具象設計論では、分類名を先に用意しない。

---

## 14. Python以外では別の表現を選んでよい

`dataclass`、`Self`、`Dependencies | None = None`、`default()`という書き方はPython向けの具体例である。

別の言語では、

- Constructor Injection
- Record
- Struct
- Optional parameter
- Default constructor
- Package-private factory

など、その言語で自然な方法を選べばよい。

具象設計論として言語を越えて残すのは、

1. 直接利用する依存だけを局所的に明示する
2. 依存を階層化して輸送しない
3. 標準構成を利用場所の近くで成立させる
4. 必要な範囲だけ差し替え可能にする
5. 利用側が期待する形を明示する
6. 依存関係をPythonのimportと直接利用の関係として追えるようにする

という性質である。

---

## 15. この章の判断原則

依存を定義するときは次の順序で考える。

1. このモジュールが直接使う外部要素は何か
2. 単純な関数引数だけで十分か
3. 複数あるならDependency型としてまとめる意味があるか
4. 利用側が必要なのは結果か、生成能力か
5. 標準構成を`default()`として持てるか
6. 配下モジュールの依存まで含めていないか
7. モジュール単体で差し替えられるか
8. 通常のimportで十分な依存までDependency型へ包んでいないか

次章では、このDependencyの標準構成と、テストで差し替える範囲を扱う。
