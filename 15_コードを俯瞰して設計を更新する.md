# 第15章　コードを俯瞰して設計を更新する

## 1. 実装しただけでは概念は発見されない

具象設計論は、具体的なコードを書くことで概念発見の材料を増やす。

しかし、コードを書けば自動的に適切な概念が見えるわけではない。

実装した後に、増えた具体例を見直す必要がある。

**具体化と俯瞰は一組である。**

---

## 2. 新しいFeatureを追加したとき

Featureを一つ追加したら、既存Featureとの関係を見る。

- 同じ業務規則が増えていないか
- 既存Featureと同じ利用目的ではないか
- 新しいData Component候補が見えていないか
- 共通化ではなく統合すべきではないか

追加したFeatureだけを完成させて終わらない。

兄弟を並べて見る。

---

## 3. 新しいData Componentを追加したとき

新Data Componentを作ったら、

- 既存Data Componentと同じ状態を二重管理していないか
- Transaction都合だけで分けていないか
- 本当に独立したライフサイクルか
- UseCaseが常に両方を同時に変更していないか

を見る。

分割した事実そのものを正解とみなさない。

---

## 4. フォルダ内のファイルが増えたとき

ファイル数は判定条件ではない。

しかしファイルが増えたことは、具体例が増えたことを意味する。

```text
data_component/job/
    domain.py
    repository.py
    schedule.py
    revision.py
    assignment.py
    calculation.py
```

となったら、名前を眺めるだけでも新しい概念候補が見える。

---

## 5. 同じDependencyが繰り返し現れたとき

複数モジュールに、

```python
clock: Clock
```

があること自体は問題ではない。

しかし、

```python
data_provider
permission_checker
notification_sender
...
```

まで常に同じ組み合わせで現れるなら、その組み合わせ自体に意味があるかを考えられる。

ただし、すぐに共通Dependenciesへ統合しない。

まずなぜ同じ組み合わせになるのかを考える。

---

## 6. 公開面が巨大化したとき

```python
__all__ = [
    "create",
    "update",
    "delete",
    "copy",
    "revise",
    "schedule",
    "notify",
    "export",
    ...
]
```

と増えたなら、その概念が多すぎる責任を持っていないかを見る。

公開面は、その概念が外部へ提供する意味の一覧でもある。

巨大な公開面は概念再評価の観測点になる。

---

## 7. 深いimportが増えたとき

他概念から、

```python
from data_component.job.internal.xxx import ...
```

が増えているなら、

- 公開面が不足している
- 利用側が内部実装へ依存しすぎている
- そもそも別概念へ分けるべき処理が内部にある

可能性がある。

深いimportを禁止するだけでなく、なぜ必要になったかを見る。

---

## 8. 条件分岐が繰り返されるとき

一つの条件分岐は、そのまま条件分岐でよい。

しかし、

```python
if customer_type == ...
```

が複数Featureへ広がったなら、独立した概念や規則が見えている可能性がある。

条件分岐数を減らすことが目的ではない。

同じ判断が繰り返されている意味を見る。

---

## 9. Test Setupが巨大化したとき

一つのモジュールをテストするために大量のFakeが必要なら、そのモジュールが多くを直接利用しすぎている可能性がある。

または、Dependencyを上位から階層的に輸送している可能性がある。

Testは依存構造の診断材料になる。

---

## 10. 変更原因と影響先を分けて見る

Jobを変更すると必ずScheduleも修正する。

Complete Jobを変更すると必ずNotification Featureも同時に変更する。

この状態が繰り返されるなら、同時に変更されたことだけで統合を決めず、最初に変わった意味を調べる。

Jobの公開面が変わり、それを利用するScheduleやFeatureの呼び出し方が変わったなら、変更原因は依存先であるJobにある。Pythonではimportと直接利用の関係を辿れば、その影響先を確認できる。

一方、別々に置いたコードが同じ状態・規則・ライフサイクルの変更によって毎回変わるなら、概念境界を統合する材料になり得る。

---

## 11. 名前が実態を説明できなくなったとき

`job`配下にCustomer管理が入り始めた。

`web`配下にCLIでも使うUseCaseが増えた。

`shared`配下に業務ルールが大量に集まった。

このように、名前から配下の意味を説明できなくなったら、構造を見直す。

具象設計論では名前をArchitectureの装飾ではなく、概念理解の表現として使う。

---

## 12. 「今の構造でも書ける」を疑う

既存構造の慣性は強い。

新しい概念が見えても、

> 今の場所でも実装できる

という理由で変更を見送れる。

しかし「書けること」と「適切な意味を表していること」は別である。

構造変更のコストと、意味のずれを残すコストを比較する。

---

## 13. Refactoringは設計活動である

具象設計論ではRefactoringを、

> 汚くなったコードを掃除する作業

だけとは考えない。

具体化によって更新された概念理解を、コード構造へ反映する作業でもある。

ファイル移動、名前変更、公開面変更、Dependency再配置は、設計そのものの更新である。

---

## 14. Agentにも俯瞰を要求する

AI Agentに具象設計を適用する場合、

> 要求された機能を実装して終わり

にしない。

実装後に、

- 新しいFeatureと既存Featureの重複
- Data Component候補
- 深いimport
- Dependencyの反復
- 公開面の増大

を再確認させる。

ただし自動的にRefactoringさせるのではなく、変更候補として判断させる運用もできる。

---

## 15. 見直しのタイミング

見直しは大規模なArchitecture Reviewだけでなく、日常的な変更時に行える。

例えば、

- 新しい兄弟フォルダを追加した
- 新しい公開関数を追加した
- Dependency fieldを追加した
- 同じ条件分岐を二度目に書いた
- Test Setupが急に増えた

といった時点で、周辺を見る。

概念発見を一度きりの上流工程にしない。

---

## 16. この章の判断原則

1. 具体化したら俯瞰する
2. 新しい兄弟を追加したら既存兄弟を見る
3. Data Componentを作ったら既存境界を見る
4. ファイル増加を観測点にする
5. Dependency・公開面・深いimportを観測する
6. Test Setupを依存診断に使う
7. 同時に変わったファイルではなく、変更原因となった意味と依存関係を見る
8. 名前と実態のずれを見る
9. 「今の構造でも書ける」だけで維持しない
10. Refactoringを概念更新として扱う

ここまでで、具象設計論の設計実践そのものを一通り扱った。

次部では、既存語彙を必要な意味で使う方法、命名、具象設計が扱う範囲を整理する。
