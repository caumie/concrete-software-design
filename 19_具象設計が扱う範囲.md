# 第19章　具象設計が扱う範囲

## 1. コード構造へ概念理解を反映する

具象設計論が中心的に扱うのは、業務やシステムについて得られた理解をコード構造へ落とし込む部分である。

- どの概念の近くへ置くか
- 何を一つのModuleとして始めるか
- いつPackageへ深掘りするか
- 何を公開するか
- 依存をどこで明示するか
- Transactionをどこで扱うか
- 増えた具体例をどう再評価するか

を扱う。

## 2. Domain理解は設計の入力になる

業務知識は、利用者への聞き取り、業務分析、既存システム、規程、運用経験などから得る。

具象設計では、その理解をFeatureやData Component、型、公開操作へ反映する。

Domain理解が更新されたら、コード側の概念も更新する。

## 3. Data Component境界は具体例から判断する

Data Componentを分けるときは、

- 状態
- ライフサイクル
- 変更理由
- 同一性
- 複数Featureから見える共通規則

などを判断材料にする。

数値的な閾値より、増えた具体例を俯瞰して意味を判断する。

## 4. Feature境界は利用目的から判断する

Featureは利用目的を中心に作る。

利用者、操作目的、成功条件、変更理由などが分かれてきたらFeature分割を検討する。

逆に、別々に作ったFeatureが同じ利用目的を表していると分かれば統合できる。

## 5. 性能設計は同じ概念構造の上で行う

SQL、Index、Cache、Denormalization、Batchingなどの性能設計は、FeatureやData Componentの意味を保ちながら追加する。

Queryでは、必要なRead Modelを効率よく取得するためにDomain Modelを経由せずSQLを直接使うこともできる。

性能上の具体的な要求も、コード配置を見直す材料になる。

## 6. Database設計はRepositoryと接続する

Table、Index、Constraint、Isolation Level、MigrationなどはDatabase設計として扱う。

Repositoryは、そのDatabase設計とData Componentの意味を接続する場所になる。

Database固有の責任が増えた場合は、必要に応じて独立した概念へ具体化する。

## 7. 分散処理では物理的な境界を明示する

複数Database、外部API、Queueをまたぐ処理では、部分成功やNetwork failureを前提にする。

必要に応じて、

- Retry
- Timeout
- Idempotency
- Outbox
- Compensation

などの責任を追加する。

外部境界が明確なら、External Service等の独立概念として配置する。

## 8. 組織の責任境界も変更理由になる

Teamや部門によって変更責任が分かれている場合、その境界はコード構造を考える材料になる。

Domain上の意味、変更理由、運用責任を合わせて見ながら、PackageやServiceの境界を決める。

## 9. 技術選定は具体化の条件になる

Python、Java、Database、Web Frameworkなど、採用技術の特性は実装方法へ影響する。

PythonではModuleからPackageへ同じ名前で成長できること、`Protocol`や`dataclass`を使えること、公開面をConventionとして扱うことなどを設計に利用できる。

本書のPython例は、この言語特性を活用した具体化である。

## 10. Security Requirementも概念へ配置する

Authentication、Authorization、Audit、Secret管理などのSecurity Requirementも、意味と変更理由に応じて配置する。

Entry固有のAuthentication、Data Componentの業務権限、External ServiceのCredential管理など、責任の違いを構造へ反映する。

## 11. Framework依存は変更範囲として扱う

FastAPIやDjango等のFramework型は、Entryなどその意味を必要とする場所で使う。

FeatureやData ComponentがFramework型を必要とするときは、その依存がどの変更範囲まで広がるかを確認する。

Frameworkとの距離も、局所性を考える一つの判断材料である。

## 12. 再利用性は意味の一致から判断する

同じ実装が複数箇所に現れたとき、意味と変更理由が一致するなら共通化する。

同じ実装でも変更理由が異なるなら、局所性を優先して重複を残すことがある。

共通化と分離のどちらも、意味を基準に判断する。

## 13. 差し替え可能性は必要な範囲で持つ

Productionの標準構成が固定的ならLocal Defaultを使える。

Runtimeで複数の実装を選択する必要がある場合は、Composition Root、Protocol、Factory等を使って差し替え範囲を広げる。

必要な柔軟性に合わせて依存構造を決める。

## 14. Pythonの公開面は規約として扱う

Pythonでは`__init__.py`を使ってPackageの公開面を示せる。

```python
from data_component.job import complete_job
```

のような利用経路を通常の入口として示す。

LintやArchitecture Testを追加すれば、規約をより強く検証できる。

## 15. 俯瞰を設計工程に含める

具体化したコードは定期的に俯瞰する。

- Featureが増えた
- Data Componentが大きくなった
- 公開面が増えた
- 深いimportが増えた
- 同じDependencyが繰り返し現れた
- Test Setupが大きくなった

といった変化を、概念を見直す観測点として使う。

## 16. 具象設計自身にも同じ判断を適用する

具象設計のために、

- Dependenciesが大量に増える
- 公開Wrapperが大量に増える
- Folder規約を守ること自体が目的になる

ようであれば、その構造も再評価する。

**必要な複雑性には名前と場所を与え、不必要な複雑性には存在理由を与えない。**

この判断は、具象設計の実装規約自身にも適用する。

## 17. この章のまとめ

具象設計論は、コード配置・依存・公開面・Transaction・概念の分割と統合を中心に扱う。

その周囲にあるDomain分析、性能、Database、分散処理、組織、Security、Framework等の設計結果を受け取り、実際のコード構造へ接続する。

次部では、これまでの原則を一つの小さなコードベースへ適用し、`app.py`からどのように成長させるかを通して確認する。
