# 第7章　標準構成とテスト可能性

## 1. 差し替え範囲を利用条件から決める

依存をどこまで差し替え可能にするかは、Productionで必要な構成とテスト境界から決める。

多くの業務システムでは、Productionで使う標準実装はほぼ固定されている。

Jobの永続化方法を、UseCase呼び出しごとに選択する必要はない。通常使うClockも一つでよい。通常使うData Providerもほぼ決まっている。

具象設計論では、この現実を設計へ反映する。

**通常使う依存には標準構成を与え、外部からの差し替えは必要な範囲に留める。**

---

## 2. `default()`はProductionの標準構成である

例えば、

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    conn: Connection
    data: Data
    retry_limit: int

    @classmethod
    def default(cls) -> Self:
        return cls(
            conn=connection_factory(),
            data=data_provider(),
            retry_limit=3,
        )
```

とする。

これは、

> Productionでは通常この構成でUseCaseを実行する

という意味を持つ。

通常の呼び出しは、

```python
execute(
    require_arg=10,
)
```

だけでよい。

UseCase内部で、

```python
if dependencies is None:
    dependencies = Dependencies.default()
```

として標準構成を成立させる。

Application最上位から毎回Dependencyを構築し、UseCaseまで運ぶ必要はない。

---

## 3. 標準構成はModuleの近傍で成立させる

依存をすべて外部で構成すると、

```python
dependencies = Dependencies(
    conn=connection_factory(),
    data=data_provider(),
    retry_limit=3,
)

execute(
    require_arg,
    dependencies=dependencies,
)
```

という外部構成も選べる。Runtimeで構成を横断的に切り替える必要がある場合は、この形に意味がある。

一方、Job Data Component内部のRepository選択まで最上位で組み立てると、その成立条件をFeatureやEntry経由で運ぶことになる。Repositoryの変更がUseCaseの構成変更として現れ、変更原因と影響経路が混ざりやすい。

標準構成は、それを使うモジュール自身の`default()`で成立させてよい。

これにより、依存先の標準構成は依存先の近傍に残り、上位Moduleは自分が直接利用する公開面だけを扱える。

---

## 4. Testでは直接依存を差し替える

モジュール単体Testで必要になるのは、

> このモジュール単体の処理をテストするために、配下の処理を実行せずに済むこと

である。

例えばUseCaseがJob Data Componentの公開関数を利用する。

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    complete_job: CompleteJob
    conn: Connection

    @classmethod
    def default(cls) -> Self:
        return cls(
            complete_job=complete_job_default,
            conn=connection_factory(),
        )
```

テストでは、

```python
calls: list[int] = []


def fake_complete_job(
    job_id: int,
    connection,
) -> None:
    calls.append(job_id)
```

とし、

```python
dependencies = Dependencies(
    complete_job=fake_complete_job,
    conn=fake_connection,
)

execute(
    job_id=10,
    dependencies=dependencies,
)
```

とできる。

これによって、Job Data Componentや実Databaseを動かさず、UseCaseの処理順序だけを確認できる。Testが差し替えるのはUseCaseの直接依存であり、その先にあるRepository等まで一括で構成しない。

---

## 5. 各Moduleは自分の依存境界でTestする

FeatureのUseCaseがJob Data Componentの公開関数をFakeへ差し替えたからといって、Job Data ComponentのRepositoryまでFeatureのテストで差し替える必要はない。

Job Data Component側はJob Data Component側でテストする。

例えば、

```python
# data_component/job/service.py

@dataclass(frozen=True, slots=True)
class Dependencies:
    repository: JobRepository

    @classmethod
    def default(cls) -> Self:
        return cls(
            repository=repository_default,
        )
```

とする。

Job Serviceのテストでは、

```python
dependencies = Dependencies(
    repository=fake_repository,
)
```

を渡す。

FeatureのテストとData Componentのテストは、それぞれ自分のモジュールの依存境界で止める。

これが、Dependencyを階層化しない理由でもある。

---

## 6. Test Setupの大きさは依存設計の観測点になる

一つのUseCaseをテストするために、

```python
ApplicationDependencies(
    database=...,
    job_repository=...,
    schedule_repository=...,
    user_repository=...,
    notifier=...,
    clock=...,
    cache=...,
    ...
)
```

のような巨大なSetupが必要になるなら、UseCaseが直接利用していない依存まで受け取っている可能性がある。

テストは正しさを確認するだけでなく、依存境界を観測する材料にもなる。

モジュール単体のテストなのに、システム全体の構成を準備しなければならないなら、局所性が失われていないかを疑う。

---

## 7. Interfaceを差し替え可能性のためだけに増やさない

Pythonでは、Fakeが同じ呼び出し方を満たせば、必ずしも明示的なInterface型を用意しなくてもテストできる。

例えば、

```python
def fake_complete_job(
    job_id: int,
    connection,
) -> None:
    ...
```

がUseCaseから必要な呼び出し方を満たしていればよい。

型安全性や複数実装の管理に価値があるなら`Protocol`などを使える。

しかし、

> MockしたいからすべてにInterfaceを作る

必要はない。

具象設計論では、Interfaceも必要な差異が生じたときに導入する役割の一つとして扱う。

---

## 8. 標準構成の実装知識は近傍に置く

例えば、

```python
@classmethod
def default(cls) -> Self:
    return cls(
        conn=postgres_connection_factory(),
        data=production_data_provider(),
    )
```

と書けば、そのモジュールはPostgreSQLやProduction Providerの存在を知っている。

この知識をそのModuleの標準構成として局所的に閉じる。

重要なのは、

- 通常使う構成がどこにあるか分かる
- テストでは差し替えられる
- その実装知識が無関係な上位へ伝播しない

ことである。

---

## 9. 依存の差し替えにロジックが必要な場合

Productionでも条件によって標準構成が変わることはある。

例えば、

```python
if mode == "archive":
    data = load_archive_data()
else:
    data = load_current_data()
```

とする。

この判断が`default()`の標準構成を作るために必要なら、`default()`内部に書いてよい。

```python
@classmethod
def default(cls) -> Self:
    if current_mode() == "archive":
        data = load_archive_data()
    else:
        data = load_current_data()

    return cls(
        data=data,
    )
```

この条件分岐に特別な設計用語を与える必要はない。

処理が大きくなり、独立した意味が見えたなら、そのとき関数へ切り出す。

---

## 10. ProviderやFactoryは結果ではなく能力が必要なときに使う

UseCaseが必要なのがDataそのものなら、

```python
data=data_provider()
```

と結果をDependencyに入れられる。

一方、UseCase実行中に複数回Dataを取得する必要があり、その取得タイミングをUseCase自身が決めるなら、

```python
data_provider: DataProvider
```

をDependencyにする意味がある。

同じように、Connectionを一つだけ使うなら結果のConnectionを持てる。

複数Connectionの生成タイミングをUseCaseが決めるならFactoryを持つ。

FactoryやProviderを使うかどうかは、抽象化の形式ではなく、利用側が何を判断する必要があるかで決める。

---

## 11. Default DependencyはHard Codingと同じではない

標準構成をモジュール内に書くことを、単純なHard Codingと同一視しない。

例えば、

```python
def execute(*args, **kwargs):
    conn = production_database.connect()
    data = production_api.fetch()
```

と関数内部へ直接書けば、テストで差し替えるためにはPatch等が必要になる。

一方、

```python
@dataclass(...)
class Dependencies:
    ...

    @classmethod
    def default(cls):
        ...
```

として、UseCaseが`Dependencies`を外から受け取れるなら、

- 標準構成は明示されている
- 通常はそのまま使える
- テストでは局所的に差し替えられる

という性質を持つ。

これにより、通常構成の明示と、必要な範囲での差し替えを両立する。

---

## 12. 差し替え可能性にもコストがある

依存を差し替えられるようにすると、

- 間接参照が増える
- 型が増える
- FactoryやProviderが増える
- 実際に何が使われるのか追いにくくなる

というコストがある。

だからこそ、すべてを差し替え可能にするのではなく、

> 単体テストや実際の複数構成で必要なところ

に留める。

**差し替え可能性も、無料の品質ではない。**

---

## 13. この章の判断原則

標準構成と差し替えについて、次のように考える。

1. Productionの標準構成がほぼ固定なら`default()`として持つ
2. 標準構成は直接利用するModuleの近傍で成立させる
3. Runtimeで横断的な構成選択が必要な場合は外部Compositionを使う
4. Module単体Testでは直接依存だけを差し替える
5. 配下Moduleの依存まで一括でMockしない
6. Interfaceは実際に意味があるときだけ作る
7. 条件分岐はまず条件分岐として書く
8. FactoryやProviderは生成能力そのものが必要なときだけ使う
9. 差し替え可能性のための間接参照コストも評価する

次章では、依存の中でも特に実行単位に関係するConnectionとTransactionを扱う。
