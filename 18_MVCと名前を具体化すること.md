# 第18章　名前を具体化すること

## 1. 役割名は、そのままフォルダ名にしなくてもよい

Model、View、Controllerなどの語彙は、システムを異なる責任へ分けるために使える。

その抽象語をそのまま使うことも、プロジェクトの具体的な名前へ置き換えることもできる。

プロジェクトで、

> Modelとは何か。
> Controllerとは何か。
> Viewとは何か。

をより具体的に説明できるなら、その名前をコード構造へ使える。

---

## 2. 一つの具体化として`data_component / feature / entry`

本書では、業務アプリケーションのブートストラップ例として、

```text
data_component/
feature/
entry/
```

を使う。

- Data Componentは、システムが扱う意味あるデータを中心にしたまとまり
- Featureは、そのデータ等を使って成立させる利用目的
- Entryは、外部からFeature等を利用する入口

である。

この三つは、一般的な業務Applicationで比較的早く見えやすく、横展開と縦深掘りの両方を行いやすい。

## 3. `model`とそのまま呼んでもよい

別のプロジェクトでは、

```text
model/
feature/
entry/
```

でもよい。

`model`という言葉を、

> このプロジェクトでは業務上の状態と規則を持つ概念群を指す

と明確に定義できるなら、それで成立する。

言葉と実体のずれを減らし、責任を想起しやすい名前を選ぶ。

---

## 4. 一般的な意味と違うなら具体名が受け入れやすい

`Controller`という言葉をWeb FrameworkのControllerと理解する人が多いプロジェクトで、システム全体の利用目的をControllerと呼ぶと混乱する可能性がある。

その場合、

```text
feature/
```

と具体名を付けた方が意味を共有しやすい。

一方、チーム内でControllerの意味を明確に定義できているなら、その名前を使ってもよい。

命名は正しさだけではなく、意味共有の問題でもある。

---

## 5. Entryの内部で具体的な入口へ分ける

本書では、Inbound境界の親概念として`entry/`を採用する。

CLIだけなら、

```text
entry/
    cli.py
```

でよい。

WebとCLIの両方があれば、

```text
entry/
    web.py
    cli.py
```

と並べる。

Webが成長すれば、

```text
entry/
    web/
        __init__.py
        routes.py
        session.py
    cli.py
```

へ深掘りできる。

入口がWebだけで、その名前が最も意味を共有しやすいプロジェクトでは`web/`をトップレベルに置く選択もある。

本書のブートストラップ例では、複数のInbound経路へ横展開できる`entry/`を使う。

命名は正しさだけではなく、意味共有と将来の構造展開の問題でもある。

## 6. `feature_admin`も独立概念になり得る

一般利用機能と管理機能で、

- 権限
- 利用者
- 操作目的
- 変更理由

が大きく異なるなら、

```text
feature/
feature_admin/
```

のような構造も成立する。

「Featureは一つのフォルダだけ」と固定しない。

具体的な意味が違うなら、違う名前を与える。

---

## 7. 名前は配下の意味を拘束する

`entry/web`と名付けたなら、その配下のコードはWeb Entryという概念で説明できることが望ましい。

Webと無関係な汎用UseCaseが増えたなら、名前と実態がずれている。

`data_component/job/`と名付けたなら、その配下のコードはJobという概念で説明できる必要がある。

名前は単なるラベルではなく、配下に置けるものを判断する基準になる。

---

## 8. 名前の意味が変わったら更新する

最初は`job`と呼んでいた概念が、業務理解の更新によって`production_order`の方が正確になったとする。

その場合、名前を変えることを検討する。

過去のコードとの互換だけを理由に、誤解を生む言葉を残し続ける必要はない。

Ubiquitous Languageが変わればコードも変わる。

---

## 9. `data_component / feature / entry`を一度使ってみる合理性はある

この構造をブートストラップとして使う理由は、二方向に成長しやすいことにある。

### 横に展開できる

新しい利用目的が見えたら、

```text
feature/
    create_job.py
    complete_job.py
```

と兄弟として増やせる。

新しいデータ概念が見えたら、

```text
data_component/
    job.py
    schedule.py
```

と増やせる。

新しい入口が必要なら、

```text
entry/
    web.py
    cli.py
```

と増やせる。

### 縦に深掘りできる

一つの概念の内部が具体化したら、

```text
feature/
    complete_job/
        __init__.py
        usecase.py
        query.py
```

のようにPackageへ成長できる。

Data Componentも、

```text
data_component/
    job/
        __init__.py
        domain.py
        repository.py
```

と深掘りできる。

EntryのWebも、

```text
entry/
    web/
        __init__.py
        routes.py
        session.py
```

へ成長できる。

つまり、

> **横方向には新しい概念を追加でき、縦方向には一つの概念を深掘りできる**

という性質を持つ。

その意味では、一度この構造から始めてみることには合理性がある。

ただし、External Serviceや管理機能など別の具体的な意味が見えたら、トップレベル概念を追加してよい。

**この三つを初期値として使い、具体化した意味に合わせて構造を更新する。**

## 10. 名前を増やすことにもコストがある

新しい概念が見えたように思えても、すぐにトップレベルの名前を増やすと全体が理解しにくくなる場合がある。

```text
data_component/
feature/
feature_admin/
operations/
integration/
workflow/
services/
```

と増えたとき、それぞれの違いを説明できるか確認する。

説明できない名前は、まだ具体化が足りない可能性がある。

---

## 11. AI Agentにとって名前は特に重要である

AI Agentは既存コードから構造を推測する。

`services/`、`utils/`、`common/`のような広い名前だけでは、何をどこへ追加するか判断しにくい。

一方、

```text
feature/complete_job/
data_component/job/
entry/web/dashboard/
```

のような名前は、配下の意味を推測する材料になる。

具象設計論をAgentへ与えるとき、名前と責任の定義は重要な設計情報になる。

---

## 12. 名前と公開面を一致させる

Job Data Componentが外部へ公開する関数も、

```python
complete_job
get_job
```

のように具体的な意味を持つ。

`process()`や`execute_service()`のような抽象名ばかりにすると、フォルダで具体化した意味を公開面で失う。

Featureの`execute()`のように、親概念名から意味が明らかな場合は短い名前でもよい。

```python
from feature.complete_job import execute
```

なら、完全名として意味が成立する。

---

## 13. 抽象的な役割を具体名へ変換する

抽象的な役割分割を出発点にしながら、現在のコードで意味を持つ名前へ変換できる。

具象設計論で扱うのは、

> 抽象的な責任分割を、現在のコードで意味を持つ名前へ具体化する

という操作である。

---

## 14. 命名の判断原則

1. 役割名は意味が合う範囲で使う
2. そのプロジェクトで意味を説明できる名前を使う
3. 一般的な意味と違うなら具体名を検討する
4. Entryは実際の入口名を使ってよい
5. 新しい意味があれば新しいトップレベル名を追加できる
6. 名前は配下に置ける責任の判断基準になる
7. 名前と実態がずれたら更新する
8. 名前を増やしすぎない
9. 公開面の名前も概念の意味と合わせる

次章では、具象設計が扱う範囲を整理する。
