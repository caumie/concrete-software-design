# 第2章　具体的落とし込みのブートストラップ

## 1. 最初は `app.py` だけでよい

具象設計論は、プロジェクト開始時に大量のフォルダを用意することを勧めない。

最初の要求が小さいなら、一つのファイルで始めてよい。

```python
jobs: list[dict] = []


def create_job(name: str) -> dict:
    job = {
        "id": len(jobs) + 1,
        "name": name,
        "status": "created",
    }
    jobs.append(job)
    return job
```

この段階で `domain/`、`repository/`、`infrastructure/`、`service/` を作る理由はない。

構造には、構造を必要とする具体的な理由が必要である。

## 2. コードが増えると未分化な責任が見えてくる

状態変更を追加する。

```python
def change_job_status(job_id: int, status: str) -> dict:
    job = next(job for job in jobs if job["id"] == job_id)

    if job["status"] == "completed":
        raise ValueError("completed job cannot be changed")

    job["status"] = status
    return job
```

さらに履歴、認可、Database、Webを加える。

```python
def change_job_status(request):
    user = authenticate(request)
    connection = connect_database()

    row = connection.execute(
        "SELECT id, status FROM jobs WHERE id = ?",
        (request.job_id,),
    ).fetchone()

    if row is None:
        raise NotFound()

    if not user.can_edit_job(row["id"]):
        raise Forbidden()

    if row["status"] == "completed":
        raise ValueError("completed job cannot be changed")

    connection.execute(
        "UPDATE jobs SET status = ? WHERE id = ?",
        (request.status, row["id"]),
    )

    connection.commit()

    return render_template(
        "job.html",
        job_id=row["id"],
        status=request.status,
    )
```

ここには、Web入力、認証・認可、Database、Job取得、状態遷移、Transaction、HTML出力が混在している。

この時点でも動作はする。しかし、変更理由の異なる責任が同じ場所に集まり始めている。

ここが構造化を始める観測点になる。

## 3. 早すぎる構造化も問題になる

一方、最初から、

```text
domain/
application/
repository/
infrastructure/
presentation/
```

を作ると、まだ存在しない差異まで先に固定する。

最初の一機能が、

```text
domain/job.py
application/create_job.py
repository/job_repository.py
infrastructure/sqlite_job_repository.py
presentation/create_job_controller.py
```

へ散らばる。

役割は明確である。しかし、Create Jobという一つの利用目的を理解するには最初から複数フォルダを横断する必要がある。

さらに後からFeatureという概念を導入したくなったとき、

> ApplicationとFeatureは何が違うのか。

という新しい問題が生じる。

具象設計論が避けたいのは、構造化の遅さだけではない。

**まだ具体化されていない分類を先に作ることで、後の概念形成を拘束すること**も避ける。

## 4. `app.py` を大きな意味に捉え直す

コードが増えたとき、`app.py` の内部にはいくつかの異なる意味が見え始める。

例えば、

- 業務状態と業務規則
- 利用目的
- 外部との入口

である。

この例では、次のように捉え直すことができる。

```text
data_component/
feature/
entry/
app.py
```

ここで `data_component / feature / entry` は具象設計論の固定テンプレートではない。

この例で現在説明できる三つの概念に名前を与えただけである。

別のシステムなら `model / feature / cli` でもよい。管理系の独立した意味が強ければ `feature_admin/` があってもよい。

重要なのは、**今説明できる意味だけをトップレベルへ置くこと**である。

## 5. なぜこの程度の粗さなのか

初期構造は、細かすぎても粗すぎても問題になる。

`app.py` 一つのままでは、コードが増えたときに責任が混ざりやすい。

逆に、

```text
entity/
value_object/
repository/
provider/
factory/
service/
query/
dto/
```

まで作れば、まだ存在しない役割を先に決めすぎる。

`data_component / feature / entry` 程度の粗い構造は、

- 異なる変更理由を大きく分けられる
- 新しい概念を兄弟として追加できる
- 内部構造はまだ決めずに済む

という性質を持つ。

具象設計論のブートストラップでは、この「まだ決めない余地」を重視する。

## 6. 最初のFeatureを作る

Create Jobを一つのFeatureとして置く。

```text
feature/
    create_job/
        usecase.py
```

```python
def execute(name: str, connection):
    connection.execute(
        "INSERT INTO jobs(name, status) VALUES (?, ?)",
        (name, "created"),
    )
    connection.commit()
```

この段階で、まだJob Data Componentを作る必要はないかもしれない。

Create Jobという具体的な利用目的だけが見えているなら、それをまず置く。

「Domain LogicらしいものがあるからData Componentを作る」という機械的な判断はしない。

## 7. 具体例が増えたときに初めて概念が見える

次に状態変更を追加する。

```text
feature/
    create_job/
    change_job_status/
```

さらに完了を追加する。

```text
feature/
    create_job/
    change_job_status/
    complete_job/
```

それぞれに、

```python
if current_status == "completed":
    ...
```

```python
if current_status != "running":
    ...
```

のような状態遷移規則が現れる。

ここで初めて、

> これはFeature固有の判断ではなく、Jobという状態を持つ概念の規則ではないか。

と判断できる材料が増える。

そこで、

```text
data_component/
    job/
        domain.py
```

を作る。

```python
class Job:
    def __init__(self, job_id: int, status: str):
        self.id = job_id
        self.status = status

    def complete(self) -> None:
        if self.status != "running":
            raise ValueError("only running job can be completed")

        self.status = "completed"
```

重要なのは、「Featureが三つになったらData Componentへ昇格する」という規則ではない。

**具体例を並べたことで、同じ状態と同じライフサイクルに対する規則だと判断できたこと**である。

## 8. Data Componentも後から分割できる

Data Componentを作ったからといって、その境界を確定したとは考えない。

Jobが成長し、

```python
class Job:
    ...
    def change_schedule(*args, **kwargs): ...
    def assign_member(*args, **kwargs): ...
    def complete(*args, **kwargs): ...
    def revise(*args, **kwargs): ...
```

と増えたとする。

Scheduleに独立した状態やライフサイクルが見えたなら、

```text
data_component/
    job/
    schedule/
```

へ分割できる。

具象設計論では、Featureだけを一時的な仮置き場と考えない。

Data Component、Feature、Entryのどれも、具体化によって再評価される。

## 9. Transactionは概念分割を妨げない

JobとScheduleを分けても、一つのUseCaseで同じConnectionを利用すれば一Transactionで扱える。

```python
def execute(command, connection):
    job = load_job(command.job_id, connection)
    schedule = load_schedule(command.schedule_id, connection)

    job.ensure_reschedulable()
    schedule.change_period(command.period)

    save_job(job, connection)
    save_schedule(schedule, connection)

    connection.commit()
```

したがって、

> 一緒にTransactionしたいから同じData Componentにしておく

とは限らない。

意味上の概念分割とTransaction境界は別の判断として扱う。

## 10. `shared`へ置く判断

状態遷移規則を一つ見つけた時点で、

```text
shared/
    job_rules.py
```

へ移すこともできる。

この配置が、その規則を広い範囲から利用できる共通概念として扱う意図を表しているなら、`shared/`へ置くこと自体は成立する。

一方、現在は一つのFeatureの意味としてしか説明できないなら、

```text
feature/
    change_job_status/
        rules.py
```

に置いておく方が、その時点の意味を表しやすい。

後から同じ意味が複数箇所で必要になったときに再評価する。

利用箇所数は判断材料にはなるが、機械的な条件ではない。`shared/`を選んだ理由と、そこで共有する意味を説明できるかで判断する。

## 11. 望ましくない早すぎる抽象化

Create Job一つに対して、

```text
command.py
handler.py
dto.py
mapper.py
service.py
repository.py
factory.py
provider.py
```

を一式作る必要はない。

必要な差異がないなら、

```python
def execute(name: str, connection):
    ...
```

でよい。

具象設計論は、役割名を先に揃える方法ではない。

**実際に存在する差異へ、必要になった時点で名前を与える。**

## 12. 初期構造から得られるもの

粗い初期構造には、次の利点がある。

### 新しい概念を兄弟として追加できる

```text
feature/
    create_job/
    complete_job/
```

```text
data_component/
    job/
    schedule/
```

```text
entry/
    web/
    cli/
```

と横に増やせる。

### 概念内部を必要になった分だけ細分化できる

単純なFeatureなら `usecase.py` 一つでよい。複雑になれば `query.py` や別の役割を追加してよい。

### 概念変更を構造へ反映しやすい

Feature内の規則をData Componentへ移す、Data Componentを分割する、Web固有だったUseCaseをFeatureへ移す、といった変更を通常の設計活動として扱える。

## 13. 初期構造が捨てるもの

意味中心の構造は、技術役割ごとの一覧性を一部捨てる。

Repositoryを役割別にまとめる構造なら、`repository/`から一覧できる。

具象設計では、

```text
data_component/job/repository.py
data_component/schedule/repository.py
```

のように意味ごとに分散する。

これは意図的なトレードオフである。

ファイルシステムではJobやScheduleという意味軸を表し、Repositoryという役割を横断して調べる必要がある場合は検索を使う。

役割横断の変更が起きないという前提は置かない。その変更は、各Moduleがどの依存を直接利用しているかという関係を辿って扱う。

現代のIDEや検索機能を使えば、役割名から横断検索することもできる。

ファイルシステムには、技術分類より意味上のまとまりを優先する。

## 14. 初期構造が保証しないもの

`data_component / feature / entry` を作っただけで、適切な概念境界が自動的に決まるわけではない。

Featureを増やし続ければ、同じ業務概念がFeatureごとに重複することもある。

Data Componentを作れば、

```text
data_component/
    everything/
```

のような巨大な概念にもできる。

構造は概念発見を保証しない。

具象設計論が提供するのは、発見した概念を受け入れられる余地である。

したがって、コードが増えたときには必ず俯瞰が必要になる。

## 15. フォルダが増えたことを観測点にする

Featureが増えたから自動的に再設計するのではない。

しかし、増えた具体例を並べて見ることはできる。

```text
feature/
    create_job/
    change_job/
    cancel_job/
    copy_job/
    revise_job/
    reschedule_job/
```

この並びを見て、

- `change_job` と `revise_job` は本当に別か
- `reschedule_job` はScheduleを中心に見るべきではないか
- 状態遷移規則が重複していないか
- `copy_job`だけ異なる責任を持っていないか

を考える。

フォルダ数は判定条件ではない。

**具体例が増えたことで、設計を再評価する材料が増えた**と考える。

## 16. ブートストラップとしての最小条件

具象設計論が初期構造に求める条件をまとめる。

1. 何も必要なければ `app.py` のままでよい
2. 異なる変更理由が見え始めたら、説明できる概念に名前を与える
3. 新しい概念を兄弟として追加できる
4. 概念内部は必要になった分だけ細分化する
5. 配下の責任が親概念の意味を越え始めたら配置を見直す
6. 既存概念の分割と統合を許容する
7. 具体例が増えたら俯瞰する
8. まだ説明できない分類のためだけにフォルダを作らない

この条件を満たしていれば、初期構造の具体的な名前はプロジェクトごとに異なってよい。

## 17. `app.py` を捉え直す

`app.py` から、

```text
data_component/
feature/
entry/
app.py
```

へ進むと、`app.py` に混在し始めた異なる意味へ、名前と配置が与えられる。

何も提示しなければ自由は最大である。しかしコードが増えれば、その自由は急速に失われる。

逆に最初から詳細なArchitectureを提示すれば、自由を失う時期が早すぎる。

その中間として、

**現在説明できる大きな概念だけを提示し、その内部と兄弟方向の両方に成長余地を残す。**

これが具象設計におけるブートストラップである。

次章からは、この構造の中で「どこへ置くか」「どう利用するか」「何を公開するか」を具体的に扱う。
