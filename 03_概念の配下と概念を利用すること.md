# 第3章　概念の配下と、概念を利用すること

## 1. 配置と利用を同じ言葉で説明しない

第2章では、`app.py` からコードが成長するとき、現在説明できる概念に名前を与え、その配下へコードを置くことを示した。

ここから先では、二種類の関係を区別する必要がある。

一つは、

> どの概念の配下にコードを置くか。

もう一つは、

> あるコードが、別の概念をどのように利用するか。

例えば、すでに内部役割が増えてPackage化された次の構造を考える。

```text
data_component/
    job/
        __init__.py
        domain.py
        repository.py

feature/
    complete_job/
        __init__.py
        usecase.py
```

`domain.py` と `repository.py` はJobという概念の配下にある。

一方、`complete_job/usecase.py` はJob Data Componentの公開機能を利用する。

この二つは異なる関係である。

本書では、**所有という語は原則として「ある概念の配下に置く」という関係だけに使う。**

依存関係については、

- 利用する
- 依存として明示する
- 公開機能を呼び出す

という言葉を使う。

この区別を曖昧にすると、「Job配下にRepositoryがあるからJob EntityがRepositoryを呼ぶ」「UseCaseがJobを使うからJobはFeatureに所有される」といった、本来別の話が混ざり始める。

---

## 2. 配下にあることは、呼び出し方向を意味しない

Job Data Componentの内部にRepositoryがあるからといって、Domain Object自身がRepositoryを呼び出す必要はない。

例えば、

```python
# data_component/job/domain.py

class Job:
    def __init__(self, job_id: int, status: str):
        self.id = job_id
        self.status = status

    def complete(self) -> None:
        if self.status != "running":
            raise ValueError("only running job can be completed")

        self.status = "completed"
```

とする。

永続化は別の役割として、

```python
# data_component/job/repository.py

def get(connection, job_id: int) -> Job:
    ...


def save(connection, job: Job) -> None:
    ...
```

と置ける。

どちらもJobという概念の配下である。

しかし、この配置は、

```text
Job Entity
    ↓
Repository
```

という呼び出し関係を意味しない。

配置が表すのは、

> このRepositoryは何についてのRepositoryなのか。

という意味である。

実際に誰がRepositoryを利用するかは、コード上の別の判断で決まる。

---

## 3. 概念はまず一つのモジュールとして置ける

新しい概念が一つのファイルで表現できるなら、最初からフォルダを作る必要はない。

例えばJobという概念が見えた時点で、

```text
data_component/
    job.py
```

と置ける。

Create Job Featureも、

```text
feature/
    create_job.py
```

でよい。

フォルダを作るのは、その概念の内部にさらに複数の役割や概念を分ける必要が生じたときである。

```text
data_component/
    job/
        __init__.py
        domain.py
        repository.py
```

```text
feature/
    create_job/
        __init__.py
        usecase.py
        query.py
```

具象設計論では、**概念を作ることと、概念のためのフォルダを作ることを同一視しない。**

まず機能や概念を作る。内部が具体化した結果として、必要ならPackageへ成長させる。

## 4. PythonではModuleからPackageへ同じ名前のまま成長できる

Pythonでは、通常の`.py`ファイルもPackageも、importする側から見ればModuleとして扱える。

例えば最初に、

```text
data_component/
    job.py
```

があり、

```python
from data_component.job import complete
```

と利用していたとする。

Jobの内部を分ける必要が生じたら、

```text
data_component/
    job/
        __init__.py
        domain.py
        repository.py
        service.py
```

へ変更できる。

`job/__init__.py`で、

```python
from .domain import Job
from .service import complete

__all__ = [
    "Job",
    "complete",
]
```

と同じ公開面を維持すれば、利用側は引き続き、

```python
from data_component.job import complete
```

と書ける。

これはPython固有の性質を利用した、具象設計と相性のよい成長方法である。

**一つの概念を一つのModuleとして始め、内部に新しい概念や役割が現れたときだけSubmoduleを持つPackageへ成長させる。**

フォルダ化すること自体がArchitectureの前進なのではない。内部を分ける具体的な理由が現れたことに価値がある。

## 5. `__init__.py`はPackage化した概念の公開面になる

一つのファイルである間は、そのModule自身が公開面になる。

```text
data_component/
    job.py
```

から、

```python
from data_component.job import complete
```

と利用できる。

内部をPackageへ分割した後は、`__init__.py`がその公開面を引き継ぐ。

```text
data_component/
    job/
        __init__.py
        domain.py
        repository.py
        service.py
```

```python
# data_component/job/__init__.py

from .domain import Job
from .service import complete_job, get_job

__all__ = [
    "Job",
    "get_job",
    "complete_job",
]
```

別概念からは、

```python
from data_component.job import complete_job
```

と利用する。

このとき利用側は、`complete_job`が内部で`repository.py`を使うのか、別のSubmoduleへ処理を委ねるのかを知る必要がない。

Pythonでは「ファイルからフォルダへ変える」ことが、そのまま外部APIの破壊を意味しない。

そのため、**概念は小さくModuleとして始め、必要なときだけPackageへ具体化する**という成長を取りやすい。

## 6. 公開面は依存禁止の仕組みではない

Pythonでは、公開面を作っても内部ファイルを直接importできる。

```python
from data_component.job.repository import save
```

とも書ける。

さらに、

```python
from data_component.job.domain import Job
```

も可能である。

フォルダ構造や`__init__.py`だけで、層をまたぐ依存を完全に禁止することはできない。

静的解析やLintによって一部を制限することはできるが、本質的にはプログラマーが依存をどう扱うかという問題が残る。

具象設計論は、この事実を隠さない。

公開面を作る目的は、

> 内部参照を物理的に不可能にすること

ではない。

> **この概念を別の概念から利用するとき、通常どこを入口にするかを明示すること**

である。

公開面は、依存を扱うための案内標識である。

---

## 7. 深い参照が増えると、概念境界が弱くなる

例えばFeatureが次のようになっているとする。

```python
from data_component.job.domain import Job
from data_component.job.repository import get, save
from data_component.job.transition import validate_transition
```

このFeatureは、Job Data Component内部の構造をかなり知っている。

内部で、

```text
transition.py
```

を `domain.py` へ統合しただけでもFeature側を変更しなければならない。

Repositoryの関数名やファイル配置を変えても同様である。

一方、

```python
from data_component.job import complete_job
```

だけで利用していれば、Job Data Componentの内部を再構成できる範囲が広い。

ここで重要なのは、ファイルを隠すことではない。

**利用側が何を知る必要があるかを小さくすること**である。

---

## 8. 公開面は概念ごとに持つ

公開面を作るという考えを、システム全体を一つのFacadeで包むことと混同してはいけない。

システム全体を一つの入口へまとめると、例えば、

```python
# application/__init__.py

def create_job(*args, **kwargs):
    ...

def complete_job(*args, **kwargs):
    ...

def change_schedule(*args, **kwargs):
    ...

def update_customer(*args, **kwargs):
    ...

def export_report(*args, **kwargs):
    ...
```

のように、無関係な概念を一つの入口へ集めることである。

これでは、どの概念の公開面なのか分からなくなる。

公開面は概念ごとに持つ。

```python
from data_component.job import complete_job
from data_component.schedule import change_period
from feature.export_report import execute
```

のように、それぞれの具体的な意味を維持する。

---

## 9. 公開型と公開関数

公開面に置くものは関数だけとは限らない。

例えば、利用側がJobそのものを必要とするなら、

```python
from data_component.job import Job
```

を公開してよい。

識別子や入力値に独立した意味があるなら、

```python
from data_component.job import JobId
```

を公開してもよい。

一方、内部の永続化表現や補助関数まで無条件に公開する必要はない。

公開するかどうかは、

> 別概念から、この意味を直接利用する必要があるか

で決める。

---

## 10. 同じ概念内部では内部参照を過剰に禁止しない

公開面は、主として概念境界を越える利用に意味がある。

同じJob Data Component内部で、

```python
# service.py
from .repository import get, save
```

とすることまで、必ず`from data_component.job import ...`へ迂回させる必要はない。

同じ概念内部のモジュール同士には、内部構造を知る合理的な理由がある。

具象設計論では、すべての参照を一種類の規則へ統一しない。

概念内部と概念外部では、必要な安定性が違う。

---

## 11. 配下の意味を越えたら配置を見直す

あるコードがどこにあるべきかを考えるとき、便利な観測点がある。

> このコードは、親となる概念がなくても同じ意味で成立するか。

例えば、

```text
entry/
    web/
        restore_session/
            usecase.py
```

がある。

このUseCaseがWeb Sessionの復旧にしか意味を持たないなら、`entry/web` 配下にあってよい。

しかし後からCLIやBatchでも同じ処理を使い始め、その処理がWebという概念から独立して説明できるようになったなら、配置を見直す材料になる。

同様に、

```text
feature/
    complete_job/
        helper.py
```

が別Featureでも同じ意味で使われ始めたなら、本当にComplete JobというFeatureの配下にあるべきかを再評価する。

ただし、利用箇所が増えたことだけで自動的に移動するわけではない。

意味が親概念を越えたかどうかを見る。

---

## 12. 「どこに置くか」の判断順序

配置を考えるとき、具象設計論では次の順序で考える。

1. このコードは何についてのコードか
2. 現在どの概念の配下なら意味を説明できるか
3. その配下で、何という役割を持つか
4. 別概念から直接利用される必要があるか
5. 利用されるなら、何を公開するか
6. 利用側は内部構造をどこまで知る必要があるか
7. 親概念の意味を越えていないか

この順序では、Repository、Query、UseCaseといった役割名だけで配置を決めない。

まず意味上のまとまりを決める。

役割は、そのまとまりの中でコードを説明するために使う。

---

## 13. 配置と依存を分離した上で、再び接続する

ここまで、配置と利用を意図的に分けて説明した。

しかし実際のコードでは両者は相互に影響する。

別概念から内部ファイルへの深い参照が大量にあるなら、その概念の公開面が不足している可能性がある。

逆に、公開関数が増え続けるなら、その概念自体が大きすぎる可能性がある。

一つのFeatureから複数Data Componentを利用することが常態化しても、それだけで問題とは限らない。複数Featureが同時に変わった場合も、Feature横断であること自体を変更原因とはみなさない。まず、共通して利用しているData Componentや外部サービスの意味・公開面が変わったのかを調べる。

Pythonでは、この依存関係は「どのModuleがどのModuleの公開要素をimportし、利用しているか」という関係としてコードに現れる。依存グラフを別の階層構造へ写し替えるより、直接のimportと利用を境界ごとに明示する。

つまり、

```text
配置
  ↕
公開
  ↕
利用
  ↕
依存
```

は独立した規則ではない。

互いを観測する材料になる。

この相互関係を扱うために、次章では利用目的を具体化するFeatureとUseCaseを取り上げる。
