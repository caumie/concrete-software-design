# 第13章　概念を分割する

## 1. 分割条件を数値で決めない

具象設計論では、

- ファイルが5個を超えた
- 500行を超えた
- 3つのFeatureから使われた

といった数値だけで概念を分割しない。

数値は観測点にはなる。

しかし、分割すべきかどうかを決めるのは、意味の違いである。

---

## 2. 変更理由を見る

一つの概念配下にあるコードが、常に異なる理由で変更されるなら、分割を考える材料になる。

例えばJob配下に、

```text
data_component/
    job/
        domain.py
        schedule.py
        delivery.py
```

がある。

`domain.py`はJob状態の変更で変わる。

`schedule.py`は工程計画の変更で変わる。

`delivery.py`は配送規則の変更で変わる。

これらが本当に同じ概念として変更されるのかを見直す。

---

## 3. 状態とライフサイクルを見る

概念分割で強い材料になるのが、状態とライフサイクルである。

Jobが終了してもScheduleは変更可能なのか。Scheduleだけを作り直せるのか。Job削除とは別にSchedule削除があるのか。

異なるライフサイクルが見えたなら、別概念として扱う理由が強くなる。

---

## 4. 同じTransactionだから一概念とは限らない

JobとScheduleを一つのTransactionで更新するからといって、一つのData Componentにする必要はない。

UseCaseが同じConnectionを持てば、別Data Componentでも一Transactionにできる。

Transaction都合だけで意味の異なる概念を結合しない。

---

## 5. 複数Featureから使われるからData Componentとは限らない

ある処理が三つのFeatureから使われた。

それだけではData Componentへ移す理由にならない。

単なる共通Utilityかもしれない。

外部基盤かもしれない。

同じ業務概念の規則かもしれない。

利用箇所数ではなく、その処理が何についての判断かを見る。

---

## 6. Featureも分割できる

一つのFeatureが、

```text
feature/
    manage_job/
        create.py
        revise.py
        cancel.py
        copy.py
```

と成長したとする。

最初は「Job管理」という一つの利用目的に見えたが、実際には異なる利用者・権限・処理順序を持つと分かったなら、

```text
feature/
    create_job/
    revise_job/
    cancel_job/
    copy_job/
```

へ分けられる。

Featureという最初の判断も固定ではない。

---

## 7. Data Componentも分割できる

Data Componentも、現在の概念理解を表す仮説である。

Job内部からScheduleという独立概念が見えたなら分ける。

```text
data_component/
    job/
    schedule/
```

分割後にUseCaseから両方を利用すればよい。

---

## 8. Entryも分割できる

Web配下が、

```text
entry/
    web/
        customer/
        admin/
```

と成長した結果、管理画面が権限・UI・利用目的の面で大きく異なるなら、

```text
entry/
    customer_web/
    admin_web/
```

のような新しいトップレベル概念を作ることもできる。

逆に同じWebとして扱う方が自然なら分けない。

入口も固定カテゴリではない。

---

## 9. 新しいトップレベル概念を追加してよい

既存構造が、

```text
data_component/
feature/
entry/
```

だからといって、新しいコードを必ずこの三つへ入れる必要はない。

例えば、独立した管理機能群が現れたなら、

```text
feature_admin/
```

を追加できる。

外部サービスとの通信が独立した意味を持つなら、

```text
external_service/
```

が必要になる場合もある。

トップレベル分類を閉じた列挙型として扱わない。

---

## 10. 分割後の公開面を見直す

概念を分割したら、以前の公開面も見直す。

例えば、

```python
from data_component.job import change_schedule
```

を、

```python
from data_component.schedule import change_period
```

へ移す。

利用側が内部移動を意識しなくてよい場合は互換用の公開関数を一時的に残すこともできる。

しかし、古い概念を永久に残すことを目的にしない。

---

## 11. Dependencyも再配置する

概念分割に伴い、依存も移す。

Job ServiceがSchedule Repositoryを直接利用していたが、Schedule Data Componentを分けたなら、Schedule側のモジュールが自分のRepository依存を持つ。

上位UseCaseはScheduleの公開関数を利用する。

Dependency階層を作らず、分割後も各モジュールで直接依存だけを持つ。

---

## 12. Testを分割の安全網にする

分割前に、

- FeatureのUseCase Test
- Data ComponentのRule Test
- RepositoryのIntegration Test

があれば、配置変更による振る舞いの破壊を検出しやすい。

具象設計論ではTestを、将来差し替え可能にするためだけでなく、概念再編を受け入れるための安全網として扱う。

---

## 13. 分割後に単純になるかを見る

分割した結果、

- 公開面が明確になった
- Dependencyが減った
- Test Setupが小さくなった
- 変更対象を探しやすくなった

なら、分割が意味を持った可能性が高い。

反対に、すべての処理で常に両方を同時に参照し、公開関数が相互に大量に呼び合うなら、分け方が適切か再評価する。

---

## 14. 分割の判断原則

1. 数値は観測点であり判定条件ではない
2. 変更理由を見る
3. 状態とライフサイクルを見る
4. Transaction都合だけで結合しない
5. 利用箇所数だけで概念化しない
6. Feature・Data Component・Entryすべてを分割可能とする
7. 新しいトップレベル概念を追加できる
8. 分割後は公開面とDependencyを再配置する
9. Testを安全網に使う
10. 分割後の局所性が本当に改善したか確認する

次章では逆に、別々に存在する概念を統合・共有するときの判断を扱う。
