# 第14章　概念を統合する・共有する

## 1. コード重複と意味重複を分ける

同じコードが二か所にある。

それだけで共通化する理由にはならない。

```python
if status == "completed":
    raise ValueError(...)
```

という同じ実装が二つのFeatureにあっても、異なる意味で変更される可能性がある。

具象設計論では、

> 実装が同じか

よりも、

> 同じ意味として一緒に変更されるか

を見る。

---

## 2. DRYを最上位原則にしない

重複コードには保守コストがある。

しかし誤った共通化にも結合コストがある。

一つの共通関数を変更した結果、意味の異なる複数Featureが同時に影響を受けるなら、その共通化は局所性を失わせている。

DRYは判断材料であり、すべての重複を消す命令ではない。

---

## 3. 同じ実装でも違う概念

例えばWeb入力とCLI入力の両方に、次のような処理があるとする。

```python
value = value.strip()
if not value:
    raise ValueError("value is required")
```

二行程度の同じ処理が存在していても、それだけで共通Utilityへ移す理由にはならない。

Web入力の空文字判定とCLI入力の空文字判定は、現在は同じ実装でも、将来は異なる入力規則へ変わる可能性がある。

実装の一致は、概念の一致を保証しない。

---

## 4. 違う実装でも同じ概念である可能性を考える

反対に、コードの実装が異なっていても、同じ概念を表している可能性はある。

ただし、実装が異なるなら結果や副作用も異なることが多いため、安易に同一概念とは判断しない。

例えばWebとBatchで別々の処理が存在するとき、

- 最終的に同じJob状態を作るのか
- 同じ業務規則に従うのか
- 異なる結果になる理由が入口の違いだけなのか
- 変更理由が本当に同じなのか

を確認する。

同じ概念かどうかは、コードの見た目だけでも、名前だけでも決まらない。

**実装差があるときは、その差が概念差なのか、入口や実行方式の差なのかを確認する。**

---

## 5. `shared/`は共有範囲を示す選択である

初期構造で、

```text
shared/
utils/
common/
```

を用意すると、配置に迷ったコードが集まりやすい。

共有概念がまだ見えていない段階では、Sharedを作らずに始めることが一つの既定値になる。

まず現在意味を説明できる場所へ置く。

ただし、プロジェクトが`shared/`を広く利用できる場所として意図的に用意する判断も許容する。`shared/`の存在自体を設計違反とは扱わない。

---

## 6. Sharedを作る場合

共有を判断するときは、例えば、

- 複数概念で同じ意味として利用する
- 独立した変更理由を持つ
- 名前を与えて説明できる

といった点を見る。これらは一律の採用条件ではなく、共有する意味を説明するための材料である。

例えばClockがシステム全体で同じ時間概念を提供するなら、

```text
shared/
    clock.py
```

が自然な場合がある。

利用箇所数だけで自動的にSharedへ移すこともしない。広い依存を許容するという判断が明示されているなら、その判断を尊重し、必要になった時点で境界を見直す。

---

## 7. Utility化で意味を消さない

例えば、

```python
def process(data):
    ...
```

のような共通Utilityへ業務処理を押し込むと、何のための処理か分からなくなる。

共通化する場合でも、

```python
calculate_business_days(...)
```

のように意味が残る名前を使う。

共通化によってコードの意味を薄くしない。

---

## 8. 共通化後も公開面を持つ

独立した共有概念になったなら、その概念も公開面を持てる。

```text
calendar/
    __init__.py
    business_day.py
```

```python
from calendar import calculate_business_day
```

と利用する。

Sharedだから内部を無制限に参照してよいわけではない。

---

## 9. Dependencyの共有と実装の共有を分ける

複数モジュールがClockをDependencyとして持っていても、Dependency定義そのものを一つの巨大な共通Dependenciesへ統合しない。

各モジュールで、

```python
@dataclass(...)
class Dependencies:
    clock: Clock
```

と定義してよい。

標準実装として同じ`system_clock`を使うことと、Dependency型を共有することは別である。

---

## 10. 共通化を戻してよい

一度Sharedへ移した処理でも、後から意味が分かれたなら再び分割してよい。

```text
shared/
    validation.py
```

が、

- Job Validation
- Schedule Validation
- Web Input Validation

という異なる責任を含むようになったなら、それぞれへ戻す。

過去の共通化を守ることを目的にしない。

---

## 11. 共通基盤が独立概念になる場合

Database接続、Authentication、Clockなどは、複数概念から使われるうちに独立した基盤概念として扱う意味が生じることがある。

その場合も、

> 技術基盤だから

ではなく、

> 独立した責任・名前・変更理由を持つ概念になった

ことを理由に構造化する。

---

## 12. 統合しすぎる兆候

次のような状態は、共通化しすぎている可能性がある。

- 共通関数に大量のflag引数がある
- 呼び出し元ごとに条件分岐が増える
- 一つの変更で無関係なFeatureのTestが壊れる
- 共通型にOptional fieldが増え続ける
- `utils.py`を読まないと多くの機能を理解できない

これらは、異なる意味を一つにまとめていないかを見る観測点になる。

---

## 13. 統合の判断原則

1. コード重複と意味重複を分ける
2. DRYを絶対視しない
3. Sharedを作らずに始めることを既定値としつつ、意図した広い共有も許容する
4. 共有する意味と範囲を説明する
5. Utility化で意味を失わせない
6. Dependency定義まで不用意に共有しない
7. 一度共通化したものも再分割できる
8. flag・Optional・条件分岐の増加を観測する

次章では、分割・統合を判断するために、コードをどのように俯瞰して設計を更新するかを扱う。
