# 第1章　具象設計論がなそうとしていること

## 1. 設計は具体化によって更新される

ソフトウェア設計では、抽象的な原則を実際のコードへ落とす段階で具体的な判断が必要になる。

この処理はどのファイルへ置くのか。新しい機能は一つのModuleとして追加するのか、既存概念へ加えるのか。Repositoryはどこで使い、どこで標準構成を決めるのか。一覧表示のQueryはどこに属するのか。複数の概念を一つの処理で更新するとき、Transactionはどこで扱うのか。

具象設計論が扱うのは、この「設計上の概念をコード配置・公開面・依存関係へ落とし込むときの判断」である。

その中心には、

> **どこまでを今決め、どこからを実装による観測に委ねるか**

という問いがある。

現在説明できる意味をコードとして具体化し、具体例が増えたらその並びを再び見る。そこで得られた理解を、次の構造へ反映する。

## 2. コードを書くことで判断材料が増える

ある状態が複数の機能から参照されて初めて、一つのデータ概念として扱う意味が見えることがある。

別々の状態だと思っていたものが、実装してみると同じライフサイクルに従っていることもある。反対に、一つのData Componentとして扱っていたものが、異なる変更理由と状態を持つ二つの概念として見えてくることもある。

事前の推測と、実装から得られる観測は相互に補完する。

具象設計論では、コードを書くことを設計上の仮説を検証する行為として扱う。

**具体化によって概念を観測し、理解が更新されたときに、その更新をコード構造へ反映できる余地を保つ。**

## 3. `app.py` が持つ自由と、初期構造が作る慣性

最も小さなプログラムは、一つのファイルから始められる。

```text
app.py
```

この状態では、構造上の前提がほとんどない。

コードが増えると、関数同士の呼び出し、Database接続、入力表現、業務規則、初期化順序などの関係が形成される。構造を変更することは可能だが、既存コードが増えるほど変更範囲も大きくなる。

そのため、最初のフォルダ構造は単なる収納方法ではない。

**初期構造は、その後どの問いを立てやすくするかを決める。**

例えば、

```text
domain/
application/
repository/
infrastructure/
```

という役割軸の構造では、新しいコードに対して「どの役割に属するか」という問いが自然に現れる。

一方、Featureや具体的なデータ概念を追加するときには、役割軸とは別の分類軸が必要になる。

具象設計論では、この初期構造が生む慣性を設計材料として扱い、最初に作る構造を小さく保つ。

## 4. 役割と、概念の配下を分ける

具象設計論では、コードについて二つの異なる問いを分ける。

第一の問いは、

> このコードは何をするものか。

第二の問いは、

> このコードは何という概念の配下にあるか。

UseCase、Query、Repository、Entity、Value Object、Controller、Providerなどは「役割」を表す語である。

一方、Job、Schedule、Complete Job、Web、CLI、Adminなどは、コードをまとめる具体的な概念になり得る。

例えば、Complete Jobという機能が一つのファイルでは説明しにくくなり、細分化された後に、

```text
feature/
    complete_job/
        usecase.py
        query.py
```

となっているなら、`complete_job` が概念のまとまりであり、`usecase.py` と `query.py` はその内部での役割である。小さい段階では`feature/complete_job.py`だけでもよい。

Queryだからトップレベルの `query/` に置くのではない。Repositoryだから必ずトップレベルの `repository/` に置くのでもない。

役割と配置を一対一に対応させるのではなく、まず意味上のまとまりを考え、その中で必要な役割を分ける。

## 5. ブートストラップとしての名前

本書では、業務アプリケーションの初期構造として、

```text
data_component/
feature/
entry/
```

を使う。

- Data Componentは「何を扱うか」
- Featureは「何をするか」
- Entryは「どこから利用されるか」

を表す。

この三つは、一般的な業務Applicationで比較的早く意味を説明しやすく、横と縦の両方向へ成長させやすい。

横には、

```text
feature/
    create_job.py
    complete_job.py
```

のように兄弟概念を追加できる。

縦には、

```text
feature/
    complete_job/
        __init__.py
        usecase.py
        query.py
```

のように、一つの概念をPackageへ深掘りできる。

プロジェクト内で別の名前の方が意味を共有しやすければ、その名前を使う。

重要なのは、**その名前が何を配下に持てるのかを説明できること**である。

名前が実態を説明しなくなったら、名前や構造を更新する。

## 6. 具体化から再抽象化へ

具象設計では、具体化と抽象化を順番に繰り返す。

まず現在分かっている意味をコードとして具体化する。

具体例が増えたら、それらを並べて見る。

同じ変更理由があるか。同じ状態を扱っているか。同じライフサイクルか。同じ利用目的か。逆に、同じ場所に置かれているコードが異なる理由で変わっていないか。

その観測から、新しい概念を作る。

```text
具体化
  ↓
観測
  ↓
再抽象化
  ↓
再配置
```

**具体化は、次の抽象化のための観測点である。**

## 7. 具象設計が扱う複雑性

ソフトウェアには、業務そのものから生じる複雑性と、コード構造や依存関係によって生じる複雑性がある。

具象設計論が主に扱うのは後者である。

例えば、一つの機能を理解するために多数の技術分類を横断する状態、依存を一つ増やすために複数階層へ引数を追加する状態、標準実装がほぼ固定されているのに構成処理が全体へ広がる状態、意味がまだ揃っていないコードを早期にSharedへ集める状態である。

具象設計論では、次の方向へ構造を寄せる。

- 現在意味を説明できる概念の近くへコードを置く
- 必要になった役割だけを分ける
- 依存は、それを直接利用するModuleで明示する
- 標準構成は局所的に解決する
- 他概念からは公開された入口を利用する
- Transactionの成功・失敗を明示的な場所で扱う
- 新しい概念を追加・分割・統合できる状態を維持する

中心にあるのは、**複雑性と、その複雑性を必要とする意味との距離を短くすること**である。

この方針を依存へ適用すると、意味でModuleを作り、そのModuleが直接利用する依存と標準構成を近傍に置く形になる。Pythonではimportと直接利用の関係が依存グラフになり、Module単体Testでは同じ境界に沿って直接依存だけを差し替える。

## 8. 概念発見と俯瞰

Feature、Data Component、Dependencyは、作った時点で完成するわけではない。

Featureが増えたら、その並びを見る。Data Componentが大きくなったら、その内部を見る。同じ依存が繰り返し現れたら、その意味が本当に同じかを見る。

具象設計では、

> 具体化する  
> 増えた具体例を俯瞰する  
> 概念を更新する  
> 構造を更新する

という循環を設計活動に含める。

構造は概念発見を自動化するものではなく、**更新された概念理解を反映できる場所**として使う。

## 9. 優先順位を決める

設計上の価値は、状況によって優先順位が変わる。

### 再利用性

同じ実装でも変更理由が異なるなら、重複を残すことがある。同じ意味として扱えることが分かった時点で共通化する。

### 差し替え可能性

Productionの標準構成がほぼ固定ならLocal Defaultを使える。Runtimeで複数構成を切り替える必要が高ければ、より外側でCompositionする。

### Frameworkとの距離

Framework固有の責任はEntry等の意味の近くへ置き、他概念へ伝播する範囲を制御する。

### 構造の統一

Featureごとに必要な内部構造は異なってよい。必要な役割だけを具体化する。

### 既存構造の維持

具体例から概念理解が変わったときは、分割・統合・移動によって構造を更新する。

## 10. 既存の設計語彙は意味が合うときに利用する

Repository、UseCase、Protocol、Adapter、Domain Eventなど、広く共有されている設計語彙は、その意味が現在のコードに合う範囲で利用する。

既知の語彙には、責任や制約を短い名前で共有できる価値がある。

具象設計論では、それらをコード配置へ接続するときに、

> いま書こうとしているコードをどこへ置くか  
> 何を公開するか  
> 何を依存として明示するか  
> どこまでを今の構造として決めるか

を具体的に判断する。

## 11. この本の中心命題

具象設計論がなそうとしていることは、次の一文にまとめられる。

> **正しい概念を最初に要求するのではなく、具体化することで正しい概念へ近づける余地を設計する。**

そのために、現在認識できる意味へ名前を与える。

必要な役割だけをその配下へ置く。

新しい意味が見えれば、新しい概念として追加する。

既存概念が大きすぎれば分割する。

別々の概念が同じ意味だと分かれば統合する。

そして、これらの変更を例外的な失敗ではなく、通常の設計活動として扱う。

次章では、この考えを何もない `app.py` から実際のフォルダ構造へ落とし込む。
