# 第11章　境界を越える依存

## 1. 境界があるからInterfaceを作る、とはしない

外部API、Mail、File、Queueなどを利用するとき、

> 外部依存だからPortとAdapterを作る

という順序にしない。

最初に考えるのは、

> 利用するモジュールは何を必要としているか

である。

例えばUseCaseがメール送信を一度呼ぶだけなら、

```python
def send_completion_mail(*args, **kwargs):
    ...
```

という関数をDependencyとして受け取るだけでもよい。

---

## 2. 利用側が期待する形を定義する

例えばUseCaseが必要なのは、

```python
send_notice(
    user_id,
    message,
)
```

という能力だけだとする。

Dependencyは、

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    send_notice: SendNotice
```

とできる。

Productionの標準構成では、

```python
send_notice=mail_sender
```

を設定する。

Testでは、

```python
send_notice=fake_sender
```

へ差し替える。

この時点で、明示的なInterfaceクラスが必要とは限らない。

---

## 3. InterfaceやProtocolが必要になる場合

複数実装が存在し、型として共通契約を確認したい場合は`Protocol`等を使える。

```python
class NotificationSender(Protocol):
    def send(
        self,
        user_id: int,
        message: str,
    ) -> None:
        ...
```

しかし、

> 外部依存だから必ずProtocol

とはしない。

Interfaceを作ることで、

- 型検査が有効になる
- 複数実装の契約が明示される
- 利用側の期待が読みやすくなる

といった具体的な価値があるときに使う。

---

## 4. Portは必要になった能力境界に名前を与える

Portという語を使うなら、

> 内部が外部へ要求する能力の境界

として扱える。

例えば、

```python
class PaymentPort(Protocol):
    def charge(
        self,
        amount: Money,
    ) -> PaymentResult:
        ...
```

がある。

しかしPortをトップレベルの`ports/`へ集める必要はない。

その能力を必要とする概念の近くに置く。

例えばPayment Featureだけが利用するなら、

```text
feature/
    payment/
        port.py
        usecase.py
```

でもよい。

---

## 5. 外部サービスという境界が最初から見えているなら、それを尊重する

外部技術だからという理由だけで、すべてを遠い`infrastructure/`へ移す必要はない。

一方で、外部サービスとの連携であること自体が、最初から十分に具体的な意味を持つ場合がある。

例えばStripe、Salesforce、Microsoft Graph、配送会社APIなどとの連携では、実装前からある程度、

- SDKへの依存
- Authentication
- Request / Responseの変換
- Timeout
- Retry
- Rate Limit
- 外部Service固有のError
- 通信失敗時の扱い

が存在すると予想できる。

この場合、

```text
external_service/
    stripe.py
```

や、プロジェクトの語彙として意味が通るなら、

```text
extend_service/
    stripe.py
```

のようなトップレベル概念を最初から置くことには合理性がある。

これは「外部技術だから技術層を作る」という判断とは少し違う。

> **Stripeという外部サービスとの通信と、その外部都合を扱う責任がすでに見えている**

ため、その意味へ名前を与えている。

反対に、あるFeatureからSDK関数を一度呼ぶだけで、独立した通信制御や変換責任がまだ存在しないなら、

```text
feature/
    payment.py
```

の中で直接利用してもよい。

具象設計論の基本は変わらない。

**まずは利用する意味の近くへ置き、具体例を観測する。**

ただし、外部サービスとの連携のように境界と責任が事前にかなり明確なものまで、無理にFeature内部へ仮置きする必要はない。

## 6. 標準実装は`default()`で選べる

```python
@dataclass(frozen=True, slots=True)
class Dependencies:
    charge: Charge

    @classmethod
    def default(cls) -> Self:
        return cls(
            charge=stripe_charge,
        )
```

とすれば、Productionで通常使う実装を局所的に示せる。

Runtimeで外側が実装を選択する必要がある場合に、外部Compositionを使う。

---

## 7. 外部型を内部へそのまま運ぶか

外部APIが、

```python
StripePaymentResult
```

という型を返すとする。

UseCaseがStripe固有情報を必要としないなら、利用側が必要な形へ変換してからDependency境界を越える方がよい。

```python
@dataclass(frozen=True, slots=True)
class PaymentResult:
    payment_id: str
    succeeded: bool
```

標準実装側で、

```python
def stripe_charge(*args, **kwargs) -> PaymentResult:
    result = stripe_api.charge(...)
    return PaymentResult(
        payment_id=result.id,
        succeeded=result.status == "succeeded",
    )
```

とできる。

これによってUseCaseはStripeの型を知らない。

---

## 8. ただしDTOを境界ごとに増やさない

外部型と内部型を分けられるからといって、境界ごとに必ず新しいDTOを増やす必要はない。

一方で、FrameworkやValidation Libraryの型を内部へそのまま持ち込まないために、Python標準の`dataclass`へ変換することは実践的な選択になり得る。

例えばWeb入力がPydantic Modelである場合、

```python
class CompleteJobRequest(BaseModel):
    job_id: int
    reason: str
```

をそのままFeatureへ渡す代わりに、

```python
@dataclass(frozen=True, slots=True)
class CompleteJobInput:
    job_id: int
    reason: str
```

へ変換する。

この変換には、

- PydanticというFramework依存をWeb側へ閉じる
- Feature側が必要とする型をPython標準型として明示する
- Validation後の値だけを内側へ渡す

という具体的な意味がある。

逆に、外部型が十分に安定しており、利用範囲も局所的で、変換によって意味が増えないなら、そのまま利用してもよい。

判断基準は「境界があるからDTOを増やす」ではない。

**型を変換することで、依存範囲や利用側の意味が実際に明確になるか**を見る。

---

## 9. RetryやTimeoutは外部サービス側の責任になりやすい

外部API呼び出しにRetryやTimeoutが必要になる。

これらが、

- 通信の一時失敗
- Rate Limit
- SDK固有の例外
- 外部Serviceの推奨Retry Policy

など、外部サービスとの接続方法そのものに由来するなら、`external_service`等の外部連携側で扱う方が自然である。

```text
external_service/
    stripe.py
```

```python
def charge(...):
    # timeout / retry / SDK error mapping
    ...
```

この種の処理をFeatureへ持ち込むと、FeatureがStripeやHTTP通信の事情を知ることになる。

一方、

> この業務処理では最大3回まで試行し、失敗したら手動確認状態へ変更する

のように、再試行回数や失敗後の処理そのものが利用目的の意味を持つなら、Feature側の判断になる。

つまりRetryという役割名だけでは配置は決まらない。

ただし外部Service呼び出しに付随する一般的な通信制御については、Featureより外部連携概念の責任になる可能性が高い。

## 10. 外部I/OとTransaction

Database Transactionの途中で外部APIを呼ぶ場合、DatabaseとAPIは同じACID Transactionにはならない。

この問題を、

```python
port.execute()
connection.commit()
```

という抽象化で消したことにはならない。

PortやAdapterは外部I/Oの物理的制約を消さない。

必要ならRetry、Outbox、Compensationなど別の責任を追加する。

---

## 11. 外部能力をSharedへ早期に集約しない

複数Featureが同じ外部サービスを利用するからといって、

```text
shared/
    external_services.py
```

のような意味の薄い共通ファイルへ集約しない。

Stripeとの連携ならStripe、Microsoft Graphとの連携ならGraphという外部サービス自体が、すでに共有される具体的な概念になり得る。

```text
external_service/
    stripe.py
    microsoft_graph.py
```

のように、それぞれの外部境界へ名前を与えた方が責任を説明しやすい場合がある。

一方、一つのFeatureから一度だけ利用しており、独立した通信責任もまだ見えていないなら、無理にトップレベル概念へ昇格させる必要はない。

**まずは利用する意味の近くへ置き、具体例を観測する。**

具体例が増え、外部サービス固有の通信・変換・失敗制御が独立した意味を持つと分かったなら、その時点で外部連携概念として切り出す。

外部サービスの場合は、その独立性が実装前から十分に予測できることも多い。そのときは最初から切り出してよい。

## 12. 境界の判断原則

外部依存について次のように考える。

1. まず利用するModuleが何を必要としているかを見る
2. Interfaceは具体的な価値があるときだけ作る
3. Portを使うなら利用側が要求する能力として定義する
4. 外部サービス固有の通信・認証・変換・失敗制御が明確なら、トップレベルの外部連携概念として先に切り出してよい
5. 単純なSDK利用だけなら、利用する意味の近くへ直接置いてよい
6. 標準実装は局所的な`default()`で持てる
7. 外部型を内部へ漏らす必要があるか判断する
8. 境界ごとにDTOを自動生成しない
9. Retry / Timeoutが通信責任か業務上の判断かを分ける
10. 外部I/Oの物理的非Atomic性を隠さない

外部境界は、他の概念より事前に見通しやすいことがある。

その場合は、具象設計論の「まだ見えていない分類を作らない」という原則と矛盾しない。**すでに見えている具体的な境界を、最初から構造として表現している**からである。

次章では、直接呼び出しではなくEventを使う場合を扱う。
