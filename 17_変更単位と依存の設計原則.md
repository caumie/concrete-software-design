# 第17章　変更単位と依存の設計原則

## 1. 意味を単位としてファイルを作る

具象設計では、まず現在説明できる意味に名前を与え、その意味を一つのModuleとして置く。

Featureは利用目的、Data Componentは意味あるデータ、Entryは利用経路を表す。さらに外部サービスや共有基盤など、別の意味が見えれば兄弟概念として追加できる。

ファイルは技術的な役割を一覧化するためではなく、何についてのコードかを示すために作る。内部の役割が増えたときだけ、同じ名前のPackageへ成長させる。

## 2. 変更単位は一種類ではない

利用目的の要求が変わればFeatureが変わる。

状態や規則が変わればData Componentが変わる。

RequestやResponseの表現が変わればEntryが変わる。

外部APIの契約や通信条件が変われば外部サービスとの境界が変わる。

具象設計論は、これらを一つの変更単位へ還元しない。変わった意味に対応するModuleを変更する。

## 3. Featureは利用目的をまとめる

Featureは、利用者またはシステムが達成しようとする目的をまとめる。

Feature固有の条件、処理順序、権限、成功と失敗の意味は、そのFeatureの変更理由になり得る。

一方、Featureが利用するData Componentや外部サービスの内容まで、Feature自身の変更理由として引き受けるわけではない。Featureは依存先の公開面を利用する。

## 4. Feature横断の変更では原因を依存先へ辿る

一つの変更で複数Featureが修正された場合、Feature横断であること自体は変更原因ではない。

例えば、複数Featureが利用するJobの規則や公開面が変わったなら、最初に変わった意味はJob Data Componentにある。各Featureの修正は、その変更が依存関係を通じて波及した結果である。

この区別をせず、横断した処理や依存先の成立条件をFeatureへ集めると、Data Componentや外部サービスの変更理由をUseCaseへ押し付けることになる。

同時に変わったファイルではなく、何の意味が最初に変わり、どの公開面を通じて影響したかを見る。

## 5. Pythonの依存グラフはimportと直接利用の関係である

Pythonでは、依存関係は「どのModuleが、どのModuleの公開要素をimportし、利用しているか」として現れる。

この関係をそのまま依存グラフとして扱う。

多くの直接依存は、配下概念の公開関数やPython標準機能である。必要な箇所ではConnection、Clock、Repository、外部Client等も直接依存になる。

依存グラフを別の階層的なDependency Objectへ写し替えることは前提にしない。

## 6. 依存は利用Moduleのトップで明示する

依存を明示する規則はUseCaseに限らない。

Feature、Data Component、Entry、Query、外部サービスなど、どのModuleでも、自分が直接利用する依存をModuleのトップで確認できるようにする。

普通のimportと型付き引数で十分なら、そのまま使う。複数依存の標準構成やテスト時の差し替え口が必要なら、Module localな`Dependencies`を置ける。

重要なのは形式を揃えることではなく、直接利用する依存と通常構成が利用場所の近くに見えることである。

## 7. 下位Moduleの依存を上位へ輸送しない

FeatureがJob Data Componentの公開関数を利用し、Job内部のModuleがRepositoryを利用するとする。

Featureの直接依存はJobの公開関数である。RepositoryはJob内部のModuleの直接依存である。

上位FeatureのDependenciesへJob Repositoryを入れず、Job側の標準構成をFeatureやEntryから組み立てない。下位Moduleは自分の依存を自分の近傍で成立させる。

これにより、Repositoryの変更をUseCaseの構成変更として扱わずに済む。

## 8. Local Defaultは標準構成を近傍に置く

Productionの標準構成がほぼ固定なら、Module localな`Dependencies.default()`で成立させられる。

Runtimeで実装を横断的に切り替える必要がある場合は、より外側で構成を選択できる。外側で扱うのは、その利用条件から外側が決める必要のある構成である。

標準構成の局所解決と外部Compositionは排他的な方式ではない。どのModuleが選択理由を持つかで配置を決める。

## 9. Testでは直接依存だけを差し替える

Module単体Testでは、そのModuleが直接利用する依存だけを差し替える。

FeatureのTestでJob Data Componentの公開関数をFakeへ替えたなら、その先のJob Repositoryまで同じTestで差し替えない。Job側はJob側のTestでRepositoryを差し替える。

この境界は、Productionの依存グラフと同じ直接利用の関係に沿っている。Test用の構成だけがアプリケーション全体へ広がらない。

## 10. Sharedへの依存は意図した選択になり得る

`shared/`は複数概念から利用される広い依存先になり得る。

広く共有する意味があり、プロジェクトがその依存範囲を許容すると判断したなら、その選択を尊重する。利用箇所数や依存方向だけで機械的に禁止しない。

一方、Sharedの名前では実態を説明できなくなった場合や、異なる意味が一つの公開面へ集まった場合は分割を検討する。

## 11. 原則は判断の既定値として使う

ここまでの規則は、例外を不可能にする制約ではない。

通常は意味でModuleを作り、直接依存を近傍で明示し、Testではその直接依存だけを差し替える。この形から外れる場合は、Runtime切替、共有基盤、Framework制約など、その選択理由をコードの近くで説明できるようにする。

設計判断には、規則を守ったかだけでなく、現在の意味と利用条件を正しく表しているかを使う。

## 12. この章の判断原則

1. 意味を単位としてModuleを作る
2. 変更されたファイルではなく、最初に変わった意味を特定する
3. Feature横断の変更原因をFeatureへ押し付けない
4. Pythonのimportと直接利用を依存グラフとして扱う
5. 依存は利用Moduleのトップで明示する
6. 下位Moduleの依存を上位へ輸送しない
7. 標準構成は選択理由を持つModuleの近傍で成立させる
8. Testでは直接依存だけを差し替える
9. Shared等への広い依存を意図した設計判断として許容する
10. 原則を機械的な禁止ではなく、判断の既定値として使う
